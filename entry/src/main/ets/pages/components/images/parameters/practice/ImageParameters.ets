@Entry
@Component
struct ImageParameters {

  build() {
    Column({ space: 40 }) {
      // 本地图片

      // 网络图片
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}